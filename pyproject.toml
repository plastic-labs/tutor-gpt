[project]
name = "tutor-gpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "honeyhive>=0.2.20",
]


[tool.uv.workspace]
members = ["agent/", "api/", "bot/"]
exclude = ["www/*"]
