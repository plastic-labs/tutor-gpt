// mimic the flow of the root page.tsx except it adds the prepended site's context
// as the first item of context in the conversation, with bloom ready to dive into the context/topic

getPromptFromUrl(url){



}